#pragma once

class Vector2
{
	float x;
	float y;

	Vector2() {}
	Vector2(float x, float y);

	// Constants
	static const Vector2 ZERO = Vector2(0f, 0f);
	static const Vector2 UNIT = Vector2(1f, 1f);
	static const Vector2 GRAVITY = Vector2(0f, -9.81f);

	// Calculation
	
	// Normal
	// Distance to
	// Etc
};